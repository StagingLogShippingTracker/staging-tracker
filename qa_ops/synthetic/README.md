# Ops (stage / ship / return) improve / training loop

Container math, shared validation, and inventory advisory smoke cases.

See `.cursor/rules/improve-loops-training.mdc` and `ops-improve-loop.mdc`.

## Commands

```powershell
python scripts/ops_improve_loop.py
python scripts/improve_loop_training.py append-lesson ops --title "..." --lesson "..."
```
