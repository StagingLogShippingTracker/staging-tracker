name: slst_wear
description: SST Wear OS client — pair, staging list, ship confirm, lean SVR.
publish_to: 'none'
version: 1.1.9+11

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.9.1
  flutter_riverpod: ^2.6.1
  shared_preferences: ^2.5.3
  intl: ^0.20.2
  http: ^1.4.0
  url_launcher: ^6.3.1
  package_info_plus: ^8.3.0
  path_provider: ^2.1.6
  open_filex: ^4.7.0
  slst_shared:
    path: ../../packages/slst_shared

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
