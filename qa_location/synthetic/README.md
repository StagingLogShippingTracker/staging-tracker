# Location / map improve / training loop

Map-first location assignment curriculum (aisle / floor / shipping / outside).

See `.cursor/rules/improve-loops-training.mdc` and `location-improve-loop.mdc`.

## Commands

```powershell
python scripts/location_improve_loop.py
python scripts/improve_loop_training.py append-lesson location --title "..." --lesson "..."
```
